import { Children } from "react";

export default function Example({ Children , variants }) {
    if variants = "default"  className =
  return (
    <button className=" bg-black border border-white ${variants}">
      <div className=" bg-white hover:bg-black">
        {/* icon in here */}
        {Children}
      </div>
    </button>
  );
}

const icons = [{name:"icon1", size:"text-2xl"}]

<Example variants = {rounded-last}>
    {
        icons.map((icon,index)=> {
            icon.
        })
    }
    {/* anythins  */}
</Example>;
